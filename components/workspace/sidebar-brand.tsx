// SidebarBrand is no longer needed — the window TitleBar handles
// traffic light clearance. Kept as a no-op export so sidebar.tsx
// doesn't need changes if re-enabled later.
export function SidebarBrand() {
  return null
}
